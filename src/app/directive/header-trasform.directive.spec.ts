import { HeaderTrasformDirective } from './header-trasform.directive';

describe('HeaderTrasformDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderTrasformDirective();
    expect(directive).toBeTruthy();
  });
});
